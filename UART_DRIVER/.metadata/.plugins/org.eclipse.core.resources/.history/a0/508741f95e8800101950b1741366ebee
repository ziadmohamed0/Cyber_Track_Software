/*
 * gpio.h
 *
 *  Created on: Sep 3, 2025
 *      Author: ziad
 */

#ifndef CUSTOM_DRIVER_MCAL_GPIO_GPIO_H_
#define CUSTOM_DRIVER_MCAL_GPIO_GPIO_H_

#include "../../lib/bit_math.h"

enum class PORT{
	A, B, C
};

enum class STATUS {
	OUTPUT, INPUT
};

class gpio{
public:
	gpio(PORT port,uint32_t pin, STATUS dir);
	void set();
	void clear();
	uint32_t get();
private:
	STATUS direction;
	PORT port_index;
	uint32_t pin_index;
};


#endif /* CUSTOM_DRIVER_MCAL_GPIO_GPIO_H_ */
